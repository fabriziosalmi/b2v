{
    "name": "b2v-docs",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "docs:dev": "vitepress dev docs",
        "docs:build": "vitepress build docs",
        "docs:preview": "vitepress preview docs",
        "test:js": "jest --coverage",
        "audit:npm": "npm audit"  // Added npm audit script
    },
    "devDependencies": {
        "vitepress": "^1.0.0"
    },
    "engines": {
        "node": "^18.0.0"
    }
}